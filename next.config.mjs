/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental:{
        appDir: true,
    },
    iamges:{
        domains:[
            "lh3.googleusercontent.com"
        ]
    }

};

export default nextConfig;
